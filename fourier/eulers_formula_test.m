% e^(i*theta) = cos(theta) + i*sin(theta)

% Test, cos(0) = 1, sin(0) = 0.
theta = 0
exp(complex(0,1*theta))

% cos(pi/2) = 0, sin(pi/2) = 1.
theta = pi/2
exp(complex(0,1*theta))


exp(complex(0,0))
exp(complex(0,-2*pi*i/1))
exp(complex(0,-2*pi*i/2))
exp(complex(0,-2*pi*i/3))
exp(complex(0,-2*pi*i/4))
